% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/processing_functions.R
\name{construct_validation_df}
\alias{construct_validation_df}
\title{Construct Validation Data Frame}
\usage{
construct_validation_df(
  detrend_win = 0,
  smooth_win = 0,
  validation_table_path,
  benchmark_folder_path,
  numeric_sentiment_df,
  is_fusion_format = FALSE
)
}
\arguments{
\item{detrend_win}{Integer. Window size in years for detrending. Defaults to 0 (no detrending).}

\item{smooth_win}{Integer. Window size in months for smoothing. Defaults to 0 (no smoothing).}

\item{validation_table_path}{Character. Path to the validation CSV table containing columns:
`indicator`, `indicator_type`, `stakeholder`, and `topic`.}

\item{benchmark_folder_path}{Character. Path to the directory containing benchmark indicator CSVs.}

\item{numeric_sentiment_df}{A data frame containing pre-processed sentiment values
with at least the columns `date`, `stakeholder`, `topic`, and `sentiment_num`.}

\item{is_fusion_format}{Logical. Whether the benchmark files are in Fusion format. Defaults to `FALSE`.}
}
\value{
A tibble including columns for original validation entries, corresponding sentiment and
benchmark data frames, and a computed `comparison_df`.
}
\description{
Builds a validation table linking sentiment indicators to benchmark economic indicators,
allowing for optional detrending and smoothing. This function processes each pair of indicators
and prepares a comparison data frame using `make_comparison_df`.
}
